version: "3.9"

services:
  front:
    container_name: dorivaldovalentim
    build:
      context: ./docker/8.3
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    networks:
      - nginx-proxy-manager
    restart: always

networks:
  nginx-proxy-manager:
    external: true